info:
  name: Create Project
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/projects
  body:
    type: json
    data: |-
      {
        "name": "Codivra Editor",
        "description": "Realtime editor",
        "language": "typescript",
        "ownerId": "cmseewoxk0000fnb8pettv96v"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
