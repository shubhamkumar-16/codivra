info:
  name: Get Session
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/auth/session
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
