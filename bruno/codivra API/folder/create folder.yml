info:
  name: create folder
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/projects/cmsfosdm80001fnukrcxnxdbx/folders
  body:
    type: json
    data: |-
      {
        "name": "src"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
